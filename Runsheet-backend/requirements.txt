# Web stack pinned to the versions the suite is actually validated against.
#
# These three had drifted from what anyone runs. requirements.txt still said
# fastapi==0.115.0 from the original project scaffold, while development and
# every local test run happened on 0.128.0 — so CI was the only place 0.115 was
# ever exercised, and a local green run was not evidence about CI. It cost us a
# real failure: tests/unit/test_device_registry.py asserted 422 == 200 in CI and
# passed locally, invisible until the pypdf fix let the run get that far.
#
# starlette is pinned explicitly rather than left to fastapi's range because it
# does the request parsing these tests assert on; leaving it floating reproduces
# the same class of drift one layer down.
fastapi==0.128.0
starlette==0.50.0
uvicorn==0.40.0
# pydantic floor is set by supertokens-python 0.31.3 (requires >=2.10.6);
# pinned to the versions the test suite is validated against.
pydantic==2.12.5
pydantic-settings==2.12.0
python-multipart==0.0.18
# ``elasticsearch==8.11.0`` was here. Nothing imports it: the document plane is
# PostgreSQL (persistence/document_store.py over the ``es_documents`` table) and
# the last ``import elasticsearch`` went with the client. Left in place it would
# be a dependency the image installs and a CVE surface nothing uses.
python-dotenv==1.0.0
litellm==1.55.0
strands-agents==1.8.0
strands-agents-tools==0.2.7
google-generativeai==0.8.0
google-cloud-aiplatform==1.72.0

# Redis for session storage (Requirement 8.4)
redis==5.2.0

# PostgreSQL: source-of-truth for transactional/financial models (Commerce
# backbone + idempotency) AND, since the Elasticsearch removal, the document
# plane too — the ``es_documents`` table is fed by the same transactional outbox
# (persistence/).
#   - SQLAlchemy 2.x async ORM (engine + session)
#   - asyncpg/psycopg3 driver for the app's async engine; psycopg3 also
#     serves Alembic's synchronous migration runner, so a single driver
#     covers both paths (postgresql+psycopg://...).
#   - alembic for schema migrations
#   - aiosqlite is a TEST-ONLY async driver so the persistence-layer unit
#     tests can run against an in-memory SQLite database with no external
#     Postgres dependency (matches the mocked-services test policy).
SQLAlchemy[asyncio]==2.0.50
alembic==1.18.4
psycopg[binary]==3.3.4
aiosqlite==0.22.1

# Rate limiting (Requirements 14.1, 14.2)
slowapi==0.1.9

# Prometheus metrics (for order_metrics.py and other service metrics)
prometheus-client==0.21.0

# OpenTelemetry for distributed tracing (Requirement 5.3)
opentelemetry-api==1.39.1
opentelemetry-sdk==1.39.1
opentelemetry-exporter-otlp>=1.28.0,<2.0.0

# Testing infrastructure (Requirement 11.6)
#
# Aligned with the versions the suite is validated against, for the same reason
# as the web stack above. CI was running pytest 8.3 / pytest-asyncio 0.24 while
# every local run used 9.0 / 1.3 — a major jump in the plugin that owns
# ``asyncio_mode=auto`` and fixture loop scoping. A test harness that differs
# from the one developers run makes a green local suite unfalsifiable evidence
# about CI, which is how a failing test sat unnoticed behind an earlier error.
pytest==9.0.2
pytest-asyncio==1.3.0
pytest-cov==7.0.0
pytest-timeout==2.4.0

# Property-based testing (Requirement 11.1)
# Aligned with the validated version; the shrinker and database format both
# moved between these, so a counterexample reproduced locally is only
# reproducible in CI on the same version.
hypothesis==6.151.4

# JWT token verification (Requirement 9.6)
python-jose[cryptography]==3.3.0

# SuperTokens identity provider / session authority (SuperTokens Auth Migration
# Req 10.1). Python SDK for the FastAPI-side integration: initializes the
# EmailPassword + Session + UserRoles recipes, serves the SDK-owned auth routes,
# and verifies SuperTokens sessions server-side against the managed SaaS core.
# Pinned for reproducible builds.
supertokens-python==0.31.3

# AWS SDK for KMS envelope encryption (Fuel Ops Hardening Req 5.1 — credentials vault)
# boto3 + cryptography are brought in transitively today; pin them explicitly
# because services/credentials_vault.py imports them directly.
boto3==1.42.39
cryptography==46.0.4

# scikit-learn for solver clustering pre-pass (Fuel Ops Hardening Req 2.3.2)
# used by Agents/support/route_solver.optimize_route_large to run a KMeans
# pre-pass on stop coordinates before per-cluster 2-opt, and by the
# prioritization DBSCAN helper (Req 3.4.1).
scikit-learn==1.8.0

# PDF generation for BOL documents (Fuel Ops Hardening Req 4.3 — services/bol_service.py)
reportlab==4.5.0

# pypdf reads the PDF that reportlab wrote, so tests/unit/test_bol_service.py can
# assert the BOL's actual rendered text rather than only the canvas call order.
# Test-only and imported lazily inside the test, but it belongs here because this
# is the sole requirements file — pytest and hypothesis are declared here too.
# It was previously undeclared: present in local venvs, absent in CI, so
# `pytest -x` aborted the whole run on ModuleNotFoundError and the coverage gate
# then failed against a partial run.
pypdf==6.10.2

# APScheduler for cron-driven integration sync runs (Fuel Ops Hardening
# Req 5.1.5 — integrations/integration_scheduler.py). Used via the
# AsyncIOScheduler + CronTrigger.from_crontab APIs to register per-
# Integration_Instance cron jobs that invoke connector.sync_pull.
APScheduler==3.11.3

# intuit-oauth (intuitlib) for QuickBooks Online Connector OAuth 2.0
# refresh-token rotation (Fuel Ops Hardening Req 5.2.2, 5.2.5; Task 9.4 —
# integrations/quickbooks_online.py). Imported lazily so unit tests that
# mock the refresh flow do not require the dependency to be installed.
intuit-oauth==1.2.6

# mygeotab SDK for Geotab Connector (Fuel Ops Hardening Req 5.4.1–5.4.5;
# Task 9.6 — integrations/geotab.py). Imported lazily: unit tests that
# mock the Authenticate flow do not require the dependency to be
# installed, but production deployments should pip-install this pin so
# the connector uses the vendor-maintained client rather than the
# HTTPS fallback.
mygeotab==0.9.1

# Stripe SDK for the Stripe Connector (Fuel Ops Hardening Req 5.5.*;
# Task 9.8 — integrations/stripe_connector.py). Imported lazily so
# unit tests that mock the SDK do not require it to be installed,
# but production deployments should pip-install this pin so the
# connector's PaymentIntent + Webhook.construct_event calls use the
# vendor-maintained client.
stripe>=10.0.0,<13.0.0

# Shapely for GeoJSON polygon validation on storm road restrictions
# (Fuel Ops Hardening Req 9.3.3; Task 10.8 — fuel/storm_mode_models.py).
# Imported lazily where used so unit tests can mock polygon validation
# without requiring the dep; production deployments should pin this so
# the StormRoadRestriction model validates WGS84 geometry robustly.
shapely>=2.0.0,<3.0.0

# pyshp for reading US Census TIGER shapefiles (state boundary detection
# in IFTA Reporter — Fuel Compliance Backbone Req 7.1, Task 12.1).
# Used by compliance/services/state_boundary_detector.py for lat/lon → state
# lookups. Imported lazily so tests can mock without requiring the dep.
pyshp>=2.3.0,<3.0.0
